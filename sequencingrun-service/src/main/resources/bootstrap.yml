spring:
  application:
    name: sequencingrunservice
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:8888
management:
  endpoint:
    web:
      exposure:
        include: info,health,env
encrypt:
  key: "Secrete Key"
